# Third-party dependencies

No vendored third-party trees are checked in. The production app uses the FastAPI upload pipeline under `backend/` and the Next.js dashboard under `src/`.

For SoccerNet / SoccerTrack reference material, clone upstream separately when needed:

```bash
git clone --depth 1 https://github.com/AtomScott/SoccerTrack-v2.git
```
