NEXT_PUBLIC_API_URL=http://localhost:8000

# Legacy upload pipeline (backend only)
# MAX_UPLOAD_MB=4096
# MAX_FRAMES=5000
# INCLUDE_MASKS_IN_API=1  # return mask_rle for player overlay (default on)
# SAM_DEVICE=auto  # cuda | mps | cpu

# ReID (backend legacy analyzer — auto-loads detetction_test/weights/osnet_x1_0_soccernet.pth)
# REID_WEIGHTS=detetction_test/weights/osnet_x1_0_soccernet.pth
